# Copy to `.env` for local development. In CI/CD, prefer --dart-define
# (see lib/config/env_config.dart) over bundling secrets into the app.

BASE_URL=http://localhost:4000/api/v1
ENVIRONMENT=development
